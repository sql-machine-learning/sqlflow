language: go

branches:
  only:
  - gh-pages
  - /.*/

service:
  - docker

script:
  - pwd
  - ls -lah
  - docker build -t sqlflow:dev -f Dockerfile.dev .
  - docker run --rm -v $GOPATH:/go -w /go/src/github.com/sql-machine-learning/sqlflow sqlflow:dev bash go test -v
